name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
    - name: rust-rustfmt-check
      uses: mbrobbel/rustfmt-check@0.5.0
